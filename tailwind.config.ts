import type { Config } from 'tailwindcss'

export default {
    content: [],
    theme: {
        extend: {
            transitionDuration: {
                "50": "50ms"
            }
        },
    },
    plugins: [],
} satisfies Config